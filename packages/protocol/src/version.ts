export const PROTOCOL_NAME = "Elas Recebem Hoje Nostr-native Receivables Protocol";
export const PROTOCOL_VERSION = "0.1.0";
export const PROTOCOL_IS_EXPERIMENTAL = true;

export const PROTOCOL_COMPATIBILITY = Object.freeze({
  major: 0,
  rule: "exact-minor" as const,
});
